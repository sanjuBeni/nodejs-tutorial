const fs = require("fs");
const path = require("path");

const dir = path.join(__dirname, "files");

// for (let i = 1; i <= 5; i++)
//   fs.writeFileSync(`${dir}/hello${i}.txt`, `This is file number ${i}`);




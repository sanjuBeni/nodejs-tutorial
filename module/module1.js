
const sum = (a,b)=>{
    return `Sum is : ${a+b}`;
}

const sub = (a,b)=>{
    return `Sub is : ${a-b}`;
}

const mult = (a,b)=>{
    return `Sum is : ${a*b}`;
}

const div = (a,b)=>{
    return `Sum is : ${a/b}`;
}

module.exports = {
    sum : sum,
    sub : sub,
    mult : mult,
    div : div
}